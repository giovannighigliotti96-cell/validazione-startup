rules_version = '2';
// Only the backend (Admin SDK, bypasses rules) touches Firestore. No client access.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
